{
  "mcpServers": {
    "sec-edgar": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "--platform", "linux/amd64",
               "-e", "SEC_EDGAR_USER_AGENT=Chinmay LastName (your@email.com)",
               "sec-edgar-mcp:latest"]
    }
  }
}
